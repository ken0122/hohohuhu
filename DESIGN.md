---
name: 呼噜呼噜
description: 透明桌面上的蓝色单眼小伙伴，先让路，再卖萌。
colors:
  settings-muted: "#52618a"
  settings-border: "#c4cee9"
  settings-error: "#a12635"
  settings-error-hover: "#fbecef"
  settings-warning: "#8a5a12"
  blue: "#4569df"
  blue-dark: "#2949b6"
  ink: "#17234b"
  muted: "#67749a"
  ground: "rgba(25, 43, 98, .18)"
  affection-shadow: "rgba(37, 64, 155, .24)"
  speech-shadow: "rgba(22, 39, 91, .16)"
  hint-paper: "#fffdf4"
  paper: "#fbfcff"
  field: "#edf1ff"
  field-focus: "#f2f5ff"
  placeholder: "#626f94"
  affection: "#6e89f1"
  night: "rgba(7, 12, 34, .82)"
  hud: "rgba(7, 12, 34, .94)"
  game-button: "rgba(15, 25, 61, .9)"
  game-blue: "#4265db"
  game-label: "#b9c6ef"
  game-hint: "#d9e0f8"
  game-button-text: "#d9e0f8"
  pellet: "#fff0a6"
  power-pellet: "#ffda67"
typography:
  settings-title:
    fontSize: "22px"
  section-title:
    fontSize: "15px"
  body:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", sans-serif'
    fontSize: "13px"
    lineHeight: "18px"
    letterSpacing: "normal"
  small:
    fontSize: "11px"
  status:
    fontSize: "11px"
    fontWeight: 500
  hint:
    fontSize: "13px"
  score:
    fontSize: "23px"
  level:
    fontSize: "12px"
rounded:
  bubble: "20px 20px 24px 24px"
  bubble-bottom: "24px"
  pet-focus: "14px"
  hint: "13px 15px 12px 14px"
  field: "12px"
  dismiss: "7px"
  game-button: "10px"
spacing:
  control-gap: "6px"
  form-top: "8px"
  hud-gap: "20px"
components:
  speech:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.bubble}"
    padding: "12px"
  chat-input:
    backgroundColor: "{colors.field}"
    textColor: "{colors.ink}"
    rounded: "{rounded.field}"
    padding: "0 10px"
    height: "32px"
  send-button:
    backgroundColor: "{colors.blue}"
    textColor: "white"
    rounded: "50%"
    size: "32px"
  send-button-hover:
    backgroundColor: "{colors.blue-dark}"
  dismiss-button:
    backgroundColor: "transparent"
    textColor: "{colors.muted}"
    rounded: "{rounded.dismiss}"
    padding: "5px 7px"
  game-exit:
    backgroundColor: "{colors.game-button}"
    textColor: "{colors.game-button-text}"
    rounded: "{rounded.game-button}"
    padding: "0 12px"
    height: "34px"
  game-exit-hover:
    backgroundColor: "{colors.game-blue}"
---

# Design System: 呼噜呼噜

本文对照 **0.4.4** 源码维护当前视觉与交互约束，不代表本轮已完成所有桌面验收。尺寸以 `src/core.js`、`src/main.js`、`src/renderer/game-state.js` 和对应 CSS 为准；使用与发布说明见 `README.md`。`PRODUCT.md` 是早期构想，不作为当前功能清单。

## Overview

**Creative North Star: "透明桌面活物"**

角色先于界面：蓝色双角单眼宠物直接站在用户桌面上，没有卡片外壳。奶白气泡只在被呼唤时展开；游戏用墨蓝夜幕暂时覆盖桌面。温柔、轻巧、能及时让路，是这套视觉语言的核心。

**Key Characteristics:**

- 蓝色角色是持续可见的品牌主体。
- 常态透明；只有聊天与游戏提供有边界的交互面。
- 系统字体、短句和克制的状态提示。
- 亲昵通过姿态、眨眼和小爱心表达。

## Colors

### Primary

品牌蓝用于发送、状态提示和交互强调；深蓝用于发送按钮悬停。角色内部蓝色渐变由 `assets/blue-one-eye-mascot.svg` 原样管理，不用按钮色重新绘制角色。爱心用更轻的蓝色。

### Secondary

奶黄用于豆豆与游戏分数；较暖的金黄区分大豆豆。

### Neutral

奶白承载聊天，墨蓝承载文字。浅蓝输入底色与内描边区分焦点。游戏遮罩与顶部计分条采用不同透明度的墨蓝，提示文字使用低层级浅蓝。

浅色与深色使用同一组语义 token：surface、ink、muted、border、field、focus、error 与 warning，而不是由各窗口分别判断主题。默认跟随 macOS，手动浅色/深色覆盖通过 Electron `nativeTheme` 驱动，并在窗口创建前应用。深色面保持蓝黑而非纯黑，正文、占位符和焦点轮廓维持可读对比；角色 SVG 与每个角色自己的互动色不因主题重绘。Pac-Man 是固定的深色夜幕场景，不随应用主题翻转。

## Typography

全局使用系统字体栈，不加载外部字体。回复正文是紧凑但可读的 body 层级，状态标签更小且加粗；游戏用独立的分数和过关字号。计分数字使用等宽数字特性 `tabular-nums`。关闭气泡的 Esc 标签为 11px。

## Layout

桌面宠物采用绝对定位而非网页栅格：普通窗口为 **132×132px**，角色图像盒为 **84×84px**，水平居中、底部留 7px。Pet 互动短句使用独立、点击穿透且不获取焦点的透明提示窗口；窗口按行数向上增长，尖尖始终锚定在角色头顶上方 2px，不挤占或扩大宠物的鼠标命中窗口。聊天窗口为 **272×242px**，气泡左右留 12px、顶部留 10px，宽 248px、高 140px，无拼接尾尖；靠屏幕边缘时由主进程按显示器工作区约束窗口位置。原生窗口坐标与 CSS 尺寸使用逻辑像素，不等同于 Retina 截图的物理像素。

游戏窗口覆盖光标所在显示器的完整边界，画布铺满窗口。顶部 HUD 横跨窗口、高 **96px**，以 `12px 28px` 内边距排列分数、轮次、速度倍率、方向键提示和退出按钮；过关短句位于 HUD 内，不覆盖游玩区域。宽度不超过 600px 时，HUD 缩小间距并将右侧操作纵向排列；这只是窄窗口适配，不代表支持手机。

## Elevation & Depth

宠物脚下用低透明度模糊椭圆暗示落地。聊天气泡采用单层轻阴影，发送按钮不加投影；HUD 以深色半透明背景与游戏区区分，不使用悬浮卡片阴影。豆豆的发光属于游戏反馈，不扩散成通用界面装饰。精确值见 `src/renderer/pet.css` 与 `src/renderer/game.js`，不以旧 sidecar 作为当前视觉依据。

## Shapes

角色保持双角、单眼、柔软下摆的 SVG 轮廓。聊天气泡使用柔软的不对称圆角，无尾尖和遮挡输入的伪元素；输入为 12px 圆角，发送为圆形，均高 32px。HUD 为直角通栏，退出按钮使用 10px 圆角。无额外卡片、侧栏或常驻控制面板。

## Components

### Characters, Appearance, Language and Shortcuts

状态栏将「角色…」「外观」「语言」「快捷键」放在同一分区并按此顺序排列；所有显示按键的操作都进入「快捷键」二级菜单。外观与语言由原生单选子菜单控制，选择后立即广播给已打开 renderer，不重载页面，不改变 Pet / Dodge / Pac-Man 模式、手动隐藏、焦点、聊天输入、角色草稿或游戏进度。偏好以版本化、原子替换的 `preferences-v1.json` 保存于 userData；损坏时回退“跟随系统”且不覆盖原文件。renderer 同时把明确的浅色 / 深色选择写入根元素主题状态，只有“跟随系统”才依赖媒体查询，确保已打开的角色库与聊天设置同步更新。

支持 `zh-CN`、`zh-TW`、`en`、`ja`、`fr`、`de`、`ru`。简繁中文界面沿用「呼噜呼噜」，其他语言显示 “Hulu Hulu”，但安装包名、npm 包、CLI 和应用标识保持兼容。所有应用自有菜单、窗口、ARIA、通知、错误、内置角色资料和新生成的角色分析使用当前语言。用户保存的角色名称、人格和台词仍是不可改写的用户数据；运行时的自主与互动气泡按当前语言从安全短句模板生成，不回写或迁移原始资料。布局为较长的德语、法语和俄语保留换行与滚动空间。

### Desktop Pet

角色实现分成静态形象、应用内置绑定和通用动作控制。`src/characters.js` 管理原角色的眼睛锚点、部件选择器与下摆形变参数；renderer 的 `character.js` 在运行时副本上标注部件，`character-motion.js` 驱动动作。互动状态同时映射到角色自身，不借用其他角色的 DOM 或全局 CSS 反应；替换前先完成新绑定，失败时保留旧角色，成功时销毁旧控制器的计时器、动画和监听器。

基础 SVG 绑定只要求安全静态形象，不添加腿、身体层或骨骼。导入角色会在 renderer 内从已确认眼睛框的解码像素中定位眼白与固定瞳孔，以采样的眼白颜色生成留有抗锯齿余量的遮片，再在真实瞳孔中心叠加应用自有可移动瞳孔；横向与纵向视线半径分别受真实眼白边界约束，纵向空间充足的角色应能明确向上看。该派生结构不持久化、不改写源图。无法可靠定位时使用眼框、头部或通用安全回退点。原角色继续使用原路径形变，不能因通用动作而改变其轮廓和步态。角色库支持彩色/单色 PNG/JPG 导入、模型分析、人工修正与确认切换，以下视觉约束仍以默认蓝色角色为准。

黑猫是首个转换样本：两只白眼、双耳、完整身体与右侧弯尾来自原图。单色转换生成独立静态 SVG，去除白底和浅灰阴影，归一到 64×64 viewBox；最长边约 56 单位、底部约 4 单位留白。`BLACK_CAT` 使用基础整体动作，并在可信运行时绑定中叠加两枚可转动瞳孔；不改写转换后的静态 SVG，也不添加眼皮或尾部骨骼。独立开发预览继续保留，角色库另提供正式操作入口。角色切换会同步改变互动语言、专属彩蛋与聊天人设，但菜单栏图标仍保持应用标识。

小太阳由桌面彩色导入测试样例产品化：黄色软体、橙色耳朵、奶黄色眼白和右侧弯尾构成简洁暖色轮廓，珊瑚色瞳孔由可信运行时绑定叠加。它的语言明亮、热情但保持短促，互动粒子使用橙金色星光；连续戳三次触发专属「三连点亮」双跳，不添加音效或持续闪烁。静态 SVG 不含固定瞳孔，因此三种模式及角色预览共用的视线与镜像控制不会出现重影。

原始 `assets/blue-one-eye-mascot.svg` 保持不变；其内嵌样式受页面 CSP 限制，运行时动画由外部 CSS 与 Web Animations API 控制，不依赖原 SVG 的循环动画。行走与奔跑不添加腿或身体层：没有明确足状特征时，矢量角色从安全 SVG 路径派生左右交替的下摆波浪，PNG 包装角色用应用生成的位移图只弯曲已确认 body 下段；已有腿部时保留整体起伏。周期分别为 680ms 与 220ms，静止后取消形变或归零位移并恢复原图。原生窗口移动由 renderer 的 `requestAnimationFrame` 触发，主进程按时间积分并约束位置，不用 CSS 动画代替窗口位移。

眼睛默认完全睁开，每隔 3.8–7.2 秒眨眼约 180ms；亲昵眼部反应约 280ms 后恢复睁开。Pet 接近或 hover 时上移、微转并轻微压缩；默认角色以三颗蓝色爱心错峰飘起，角色绑定可为同一抚摸反馈提供自己的符号、颜色与阴影，黑猫使用暖金色的星光与爱心，小太阳使用橙金色星光与太阳。无互动、无移动且光标不靠近时，间隔 8–15 秒轮换张望、伸展、轻弹与摇摆；动作可附带一条无任务语义的短语气词。Pet 的互动提示和自主短句使用奶白漫画气泡、墨蓝描边、硬偏移阴影与朝向角色的尾尖；中日文保持紧凑宽度、每行最多 6 个字符，英语、法语、德语和俄语使用约 220px 的宽气泡并按单词换行。窗口在屏幕边缘向内约束时，尾尖仍锚定角色头顶；行数增加只向上生长。自主动作结束后气泡额外保留 900ms，互动动作结束后额外保留 1100ms；新互动、聊天、隐藏或模式切换仍会立即清除它。聊天气泡仍保持无尾尖的输入面。摸头、挠痒、戳肚子、贴贴和长按抱抱有独立短反馈。

支持眼神的角色在 Dodge、Pac-Man 和角色预览中持续朝向光标；Pet 中按真实屏幕坐标记录光标活动，连续静止超过 5 秒后发送空视线让瞳孔回中，任意 1px 移动即在下一渲染帧恢复注视。桌面以实际窗口中的眼睛锚点计算，游戏以角色当前位置计算。活动中的光标视线按帧更新、不叠加 CSS 追赶过渡，并独立于身体速度、聊天停稳和菜单状态。所有角色在明确水平移动时镜像朝向移动方向，静止或纯垂直移动时保留最后朝向；镜像位于独立运行时层，瞳孔用反向局部偏移补偿，最终屏幕视线不会颠倒。Dodge 点击穿透且持续可见，快速逼近触发带冷却与衰减的弹射；Pet 不自主散步，复用无漫游的靠近让路与快速反射。Pet 的稳定停留点只由初始化、拖拽和方向键移动更新，自动避让、hover、菜单和模式切换不得用偏移后的当前位置覆盖它；首次产生实际避让速度时以该稳定点作为回位目标，避让速度归零后开始 3 秒计时，期间若再次产生避让速度则重新计时。光标在安全距离外继续移动不影响回位，回位中若新威胁靠近则中断并继续躲闪；hover、拖拽、方向键、聊天、隐藏和模式切换取消当前回位，但不改变稳定点。指针进入角色区域后立即停下，把优先级交还给摸头、长按和拖拽；方向键始终覆盖自动避让。Pet / Dodge 切换保留惯性，回到上次 Pet 停留位置时减速。拖拽阈值为 6px、长按约 650ms，拖拽不得兼触点击或抱抱；聊天、手动隐藏与模式切换需结束当前拖拽。

系统「减少动态效果」关闭自动眨眼、身体路径形变、稀疏自主动作与 Dodge 弹射，保留普通避让和用户控制移动；CSS 互动动画停用或显著缩短。手动隐藏独立于模式，不能因打开菜单、模式循环、宠物切换或自动恢复而自行现身。隐藏 / 显示与「召唤宠物」共用 `Control+Alt+B`；从隐藏状态重新显示时将宠物重置到当前屏幕右下角的初始位置。宠物切换默认为 `Control+Alt+Command+C`，有 400ms 防连发，只替换形象与角色资料，不改变模式和窗口状态。Pet 与 Pac-Man 共用可见性意图和低频意外隐藏恢复检查；watchdog 不抢焦点，也不更改仍有效的游戏窗口尺寸。

隐藏动效以原 SVG 的运行时副本取样，95ms 蓄力膨胀并出现放射裂纹，随后轮廓破碎成高不透明度的蓝白三角、条片与菱片，带轻微旋散和双层扩散光环；总长仍为 420ms，主进程 460ms 兜底。立即释放鼠标命中；恢复、聊天或选模式会取消未完成效果，旧回调不能再隐藏新状态。减少动态效果启动前开启或运行中开启均跳过/结束粒子；游戏一并暂停，恢复保留进度。源 SVG 不变。

菜单栏使用源角色衍生的细轮廓与单眼，黑色透明模板图由 macOS 自动着色；`Pet · 互动与移动 ⓘ` 使用 macOS 原生菜单 tooltip 承载操作说明，宠物按钮本身不设置会覆盖角色的 HTML `title`；末项「退出呼噜呼噜」是无图标的普通命令，避免系统 quit role 附带图标。

### Character Library

状态栏「角色…」打开独立原生窗口（820×800px，最小 720×620px），使用共享的浅色 / 深色语义背景、文字、边框、输入与蓝色主按钮 token。标题为 22px、角色名为 16px，正文为 13px，辅助文字为 10–12px。顶部只保留标题与添加入口；左侧本地列表和右侧预览以细分隔线区分，不重复陈述本机存储、格式限制或操作步骤。预览台使用当前主题的 field 色，并用角色脚下的低透明度落地阴影延续桌面活物感。窗口高度不超过 680px 时缩短上下留白并将放大预览从 144px 收至 112px；右侧内容独立滚动，底部操作保持可见。这是一处按需打开的普通管理窗口，不持续置顶，也不成为桌面宠物的常驻外壳。

列表区分「内置」「已添加」和「当前」，浅蓝底与蓝色内描边表示当前预览项；角色名旁用一条短状态说明预览与当前角色的关系。点击列表只预览，必须点击「使用角色」才切换，已是当前角色时主按钮显示「正在使用」。右侧先展示放大形象和一行气质 / 彩蛋摘要，再并排展示桌面 84px 与游戏 64px 实际尺寸和紧凑动作控制；动作区固定为两行三列，第一行是静止、行走、奔跑，第二行是轻跳、彩蛋、暂停 / 恢复，长语言允许按钮增高但不能各自错位换列。常态不显示视线、能力和格式教学文案，操作结果、错误与导入编辑信息按需出现。预览共用角色动作控制及系统「减少动态效果」设置；所有角色使用同一朝向与光标视线规则。添加角色显示已确认部件数量，但明确部件框只参与互动命中、动作原点和安全瞳孔定位，不代表像素分割或可独立活动的骨骼。

添加接受彩色或单色静态 PNG/JPG，硬性要求为单个完整角色、透明或一致纯色背景、正面或轻微侧面、轮廓及部件清楚。草稿并排展示原图与规范化结果，并明确“新增不会覆盖当前角色”；角色名称上限 40 个 UTF-16 代码单元。模型给出质量、气质、互动短句、彩蛋和归一化部件框；用户可逐项修正。`reject` 阻止保存，`warn` 保留人工确认路径。主按钮在新角色草稿中为「添加并使用」，编辑已有本地角色时为「保存修改」。本地角色支持新增、选择、编辑和删除；内置角色只可选择。换图、关闭或 Esc 遇到未保存内容时先确认。所有控件保留键盘焦点轮廓，列表支持上下方向键及 Home / End，状态区礼貌播报结果；切换形象不重开游戏、不清空聊天，也不唤醒手动隐藏的宠物。

### Speech Bubble

从角色头顶向上展开，底部为变换原点。Dodge 聊天只保留光标避让和衰减反射，不自主散步；光标在气泡内时停稳，气泡外点击穿透。聊天原生窗口与物理角色锚点一起移动，并整体约束在显示器工作区内。状态标签、短回复和单行输入依次排列；回复区使用礼貌的实时播报。关闭时气泡设置 `inert` 与 `aria-hidden`，不能留在键盘焦点序列中。等待状态让单眼标记轻轻呼吸并禁用输入和发送按钮，回复和错误都留在同一气泡中。当前 renderer 对请求失败显示统一错误提示，不区分鉴权、限流与超时。

聊天只在用户提交后由主进程请求固定的 `deepseek-v4-flash`；主进程根据当前已校验角色资料派生人格提示词，renderer 不能指定人格。角色图片仅在用户明确选择后由主进程请求独立的 `deepseek-v4-flash-vision-exp`，模型结果必须经过白名单、长度和数值范围校验，并在保存前提供人工修正。不启动 Claude CLI，不向 renderer 暴露凭据，也不持久化对话。输入上限 500 个 UTF-16 代码单元，回复最多 50 个可见字符；交互设计不得假设连续多轮上下文或工具调用。

### Input & Buttons

输入聚焦时浅化背景并显示品牌蓝内描边。聊天气泡与聊天设置窗口共用原生编辑快捷键处理，支持全选、复制、粘贴、剪切、撤销和重做。发送使用向上箭头图标，hover 加深、键盘聚焦显示外轮廓；禁用时降低不透明度。Esc 是气泡右上方的轻量关闭按钮。游戏退出按钮保留清楚的文字动作与焦点轮廓。

### Game Canvas & HUD

同一角色以 **64px** DOM / SVG 盒叠加在 canvas 上，运行时移除眼皮节点；canvas 绘制圆豆豆，普通与大颗分别计 1 分与 5 分，吃到时角色短促放大。初始速度 280px/s，每清完一屏乘 **1.3**，重新开局重置。方向键改变持续移动方向，左右穿屏，上下在 HUD 下方的游玩区域反弹。

HUD 不承担模式导航。过关提示在 HUD 内淡入淡出，持续 2.4 秒；减少动态效果时改为静态可见。豆豆呼吸在游戏加载时读取减少动态效果偏好；若设置在开局后变化，该项当前需要重开游戏才更新。游戏移动与吃豆缩放反馈仍保留，不宣称整个游戏完全静态。窗口缩放时，剩余豆豆和角色按可玩区域重排，保留分数、倍率、方向；不会把重排路径作为吃豆轨迹。反弹和穿屏保留本帧剩余路程，并仅对真实经过的分段路径检测碰撞。

## Do's and Don'ts

### Do:

- Do 保留透明桌面与原角色轮廓，让交互面按需出现。
- Do 用系统字体、短句和清晰焦点反馈完成轻量交互。
- Do 将蓝色用于角色和动作，将奶黄保留给游戏食物与分数。

### Don't:

- Don't 把宠物包进常驻卡片、工具栏或应用窗口外壳。
- Don't 将 SVG 角色内部渐变替换成通用按钮配色。
- Don't 添加未实现的导航、卡片库、手机断点或状态能力。

### API Settings

状态栏「聊天设置…」打开独立 480×510 逻辑像素的普通原生窗口，复用奶白背景、墨蓝文字、系统字体与蓝色主按钮，不持续置顶，也不改变宠物模式或手动隐藏状态。页面仅含接口地址、密码类型的 API 密钥、状态提示及清除 / 关闭 / 保存操作。已保存密钥不回显，留空保留；保存后清空输入，错误在表单中播报。关闭与 Esc 放弃未保存内容。只支持官方 DeepSeek HTTPS 地址，保存不等于连接验证。
